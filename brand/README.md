# Brand assets

The GroundPulse mark, rendered from the inline SVG in `../index.html` (the same
path data, so the nav logo and these files never drift).

| File | Use |
| --- | --- |
| `groundpulse-mark-1024.png` | GitHub organization avatar — upload this one |
| `groundpulse-mark-512.png` | General-purpose square logo |
| `groundpulse-mark-180.png` | `apple-touch-icon` |
| `groundpulse-mark-1024-transparent.png` | Over a non-ink background |

Mark is `#ffffff` on `--ink` `#13202c`. Regenerate with `brand/render.py`.
